# Database Configuration
# MongoDB is optional - app will use memory storage as fallback
MONGODB_URL=mongodb://localhost:27017/DUSK

# Required: Session secret for secure sessions (generate a random string)
SESSION_SECRET=your-very-secure-random-secret-key-here

# Server Configuration
PORT=5000
NODE_ENV=development

# Optional: Google OAuth (currently disabled)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: Email Configuration (if using email features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880

# Note: Replit-specific variables are automatically set in Replit environment
# Local development doesn't need these variables:
# REPL_ID=
# REPLIT_DOMAINS=